export interface Storage {
  label: string;
  value: number;
}

export interface ClientLocation {
  name: string;
  value: number;
}

export interface ComparisonChartData {
  currentYear: number[];
  lastYear: number[];
}
